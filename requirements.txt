# Privacy-Safe OCR + PII Anonymization Service
# Core runtime dependencies

# OCR
pytesseract>=0.3.13
Pillow>=10.0.0

# PII detection & anonymisation (Microsoft Presidio)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
presidio-image-redactor>=0.0.55

# NLP engine (required by Presidio Analyzer)
spacy>=3.7.0

# Web framework
Flask>=3.0.0
flask-cors>=4.0.0

# HTTP client (CLI tools + Presidio transitive dep)
requests>=2.28.0

# Testing
pytest>=7.0.0
pytest-mock>=3.0.0
